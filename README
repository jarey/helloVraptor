SMARTCLINIC
--------------------------------------------------------------------------------

IMPORTING THIS PROJECT ON ECLIPSE

In order to import this project on Eclipse, you'll need:
	* choose menu file/import existent projects
	* choose the directory that contains hellowVraptor
	* with Maven installed execute: mvn jetty:run
	* done! check the log files. access http://localhost:8080/smartclinic/
    
Tests
    mvn test
Clean
    mvn clean